import styled from "styled-components";
import Grid from "hedron";

export const GridBoxStyled = styled(Grid.Box)`
  display: flex;
  flex-direction: column;
  background-color: #df9047;
  box-shadow: 10px 0px 15px 0px rgba(0, 0, 0, 0.6);
  padding: 0;
`;
